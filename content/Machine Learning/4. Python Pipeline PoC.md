---
title: 4. Python Pipeline PoC
publish: true
---
Proof of concept-test af en model til genkendelse af bilmærker. Selvom modellen ikke var særlig succesfuld ift. den specifikke opgave, fik jeg en idé om kompleksiteten i at implementere allerede eksisterende modeller i en python-pipeline. Vigtigst af alt blev det klart, at det faktisk var realistisk i betragtning af vores begrænsede viden på området, og jeg har tillid til at vores generelle tilgang til ML-delen af projektet faktisk giver mening. Så selv om modellen stort set var ubrugelig, var eksperimentet i sig selv det ikke. Nedenfor er et udklip af en masse dårlige forudsigelser:

![[Pasted image 20250922213304.png]]

Jeg har også eksperimenteret med et undersystem til genkendelse af farver baseret på HSV-dominerende pixelværdier i et beskåret billede af en bil. 

```python
def detect_color(image, mask):

    # ── 1. Validate mask ───────────────────────────────────────────────
    if mask is None or np.count_nonzero(mask) == 0:
        return "unknown"

    # ── 2. Optional spatial crop ───────────────────────────────────────
    # Optional: chop off top 30% of image + mask to ignore roof reflections etc.

    chop_top = 0.3
    chop_sides = 0.15
    chop_bottom = 0.2

    h, w = image.shape[:2]
    top = int(h * chop_top)
    bottom = int(h * (1 - chop_bottom))
    left = int(w * chop_sides)
    right = int(w * (1 - chop_sides))

    image = image[top:bottom, left:right]
    mask = mask[top:bottom, left:right]
    cv2.imwrite(unique_filename("processed/CDcrop.jpg"), image)
    cv2.imwrite(unique_filename("processed/CDMaskcrop.jpg"), mask)

    # ── 3. HSV conversion + masking ────────────────────────────────────
    # Ensure mask is boolean
    mask = mask > 0

    # Convert to HSV
    hsv = cv2.cvtColor(image, cv2.COLOR_BGR2HSV)
    h, s, v = cv2.split(hsv)

    # Apply mask to each channel
    h = h[mask]
    s = s[mask]
    v = v[mask]

    # ── 4. pixel classification ────────────────────────────────────────
    total = s.size
    black_pixels = np.sum(v < 50)
    gray_pixels = np.sum((v >= 50) & (v < 200) & (s < 40))
    white_pixels = np.sum((v >= 200) & (s < 40))
    color_pixels = np.sum((s >= 40) & (v >= 40))

    print(f"""
White pixels: {round(white_pixels/total*100)}
Black pixels: {round(black_pixels/total*100)}
Gray pixels: {round(gray_pixels/total*100)}
Color pixels: {round(color_pixels/total*100)}""")

    # ── 5. neutral color heuristic ─────────────────────────────────────
    # Kinda white hack (the honda fix)
    neutral_colors = {
        "white": white_pixels,
        "gray": gray_pixels,
        "black": black_pixels,
    }
    dominant_neutral = max(neutral_colors, key=neutral_colors.get)
    dominant_value = neutral_colors[dominant_neutral]

    if (white_pixels > total * 0.25):
        return "white"

    if (
        dominant_value > 0.4 * total
        or (dominant_value > 0.35 * total and color_pixels < 0.65 * total)
        or (dominant_neutral == "white" and dominant_value > 0.125 * total and white_pixels > black_pixels + gray_pixels)
    ):
        return dominant_neutral

    if color_pixels < 0.4 * total and dominant_value > 0.2 * total:
        return dominant_neutral

    # ── 6. hue-based color categorization ──────────────────────────────
    hue = h[(s > 40) & (v > 40)]
    if len(hue) == 0:
        return "unknown"

    hist, _ = np.histogram(hue, bins=180, range=(0, 180))
    mode_hue = int(np.argmax(hist))

    print(f"""Mode_hue (for color categorization): {round(mode_hue)}
""")

    if mode_hue < 10 or mode_hue > 160:
        return "red"
    elif mode_hue < 22:
        return "orange"
    elif mode_hue < 32:
        return "yellow"
    elif mode_hue < 45:
        return "lime"
    elif mode_hue < 85:
        return "green"
    elif mode_hue < 125:
        return "blue"
    elif mode_hue < 160:
        return "purple"
    else:
        return "unknown"
```

Jeg legede med forskellige tærskler for kategorisering af farver, forskellige tilgange til optimering af lysstyrke og forskellige måder at inddrage lysstyrkeværdier på for at opnå nøjagtige aflæsninger. I sidste ende var resultaterne anstændige for testbillederne - jeg er klar over, at dette ikke nødvendigvis kan generaliseres:

![[Pasted image 20250922220128.png]]

Sliden er fra en forelæsning fra kurset på Stanford University - selv om det handler om hyperparametre, gælder pointen om ikke at stole på testdata til validering også i dette tilfælde.

Jeg hart også eksperimenteret med objektdetektionsmodeller (YOLO, mmdetection) og *optical character recognition* (OCR) modeller (PaddleOCR). Modellerne endte med at køre og jeg fik dem eksporteret til onnx-format, som er muligt at få til at køre på telefoner, hvilket er nødvendigt for vores projekt. Det var mest et proof of concept-arbejde for at finde ud af, om vi kunne få modellerne til at fungere i et miljø, der kunne overføres til telefoner.
### Resultater

Originalbillede:
![[car1 1.png]]

Lysstyrkejusteret og beskåret baseret på bilgenkendelse:
![[crop.jpg]]

Bilmaske:
![[crop-mask.jpg]]

Simpelt zoom hack til fargenkendelse. Fordi vi vitterligt bare kigger på hver pixels farve og vælger den mest repræsenterede farve, gør det metoden lidt mere præcis at beskære bilbilledet yderligere, så man f.eks. ikke tæller ruderne med. Det er ikke en særlig robust metode, men hvis slutbrugerne tager billeder på en konsekvent måde, er på det den anden side heller ikke ubrugeligt.

![[CDcrop.jpg]]

Zoomet maske, for at undgå at læse farve som ikke er en del af bilen (ikke det bedste eksempel, men det øverste højre hjørne viser alligevel logikken - de sorte pixels er ikke en del af bilen, så farvedetektering vil ikke omfatte disse pixels. Dette forhindrer, at baggrundsfarven inkluderes, når der registreres farve):
![[CDMaskcrop.jpg]]

Gråtonet nummerplade for at undgå baggrundsfarvestøj og forbedre kontrasten mellem tegn og ikke-tegn.

![[plate-crop.jpg]]

Resultater fra kørsel af hele pipelinen. Både nummerplade og farve registreres korrekt:

![[Pasted image 20250922220951.png]]
### Reflektioner
Det lykkedes os at få en proof of concept-version til at fungere. Det viste, at vores tilgang var realistisk og værd at bruge fremadrettet. 

Et problem, vi stødte på, var, at nogle af de modeller, vi har eksperimenteret med, falder ind under licenser, der begrænser os på nogle måder. Vi har derfor undersøgt alternativer og fået en alternativ model til at fungere til genkendelse af biler på billeder. Den eneste brik i puslespillet, der stadig er afhængig af begrænsede licenser, er nummerpladegenkendelsen. En måde at komme udenom dette på, er ved at finjustere den model, vi i øjeblikket bruger til bilgenkendelse, så den også kan genkende nummerplader. Det virker ikke urealistisk at opnå, men vi er nødt til at undersøge det nærmere.

Vi fandt også ud af, at Apple ikke tillader at Python-kode kører på deres telefoner. Det er et ret stort problem for os, og vi er ved at undersøge, hvordan vi kan komme uden om dette problem. Dette problem tager førsteprioritet lige nu, fordi vi er nødt til at løse det for at få en MVP i hænderne på brugerne til test. De modeller, vi bruger i øjeblikket, har ingen licens-begrænsninger ift. brugertest, så vi kan bruge den nuværende opsætning til brugerfeedback, selv om de endnu ikke er optimeret eller perfekt implementeret endnu. Så selv om modellerne ikke er produktionsklare, kan den nuværende opsætning være god nok til brugertest. Intern tests på telefonerne vil blive udført før brugertests.